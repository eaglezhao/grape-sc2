<Window x:Class="Vestras.StarCraft2.Grape.TestApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Grape Test Application" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        <ToolBar Height="30" Grid.Row="0">
            <Button Content="Open" x:Name="openButton" Click="openButton_Click" />
        </ToolBar>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="250" />
            </Grid.ColumnDefinitions>
            <TextBox IsReadOnly="True" x:Name="fileContent" Grid.Column="0" />
            <TreeView x:Name="treeView" Grid.Column="1" />
        </Grid>
        <Grid Height="35" Grid.Row="2">
            <Button Content="Close" Width="75" Height="25" HorizontalAlignment="Right" Name="closeButton" Click="closeButton_Click" Margin="0,5,6,5" />
            <Button Content="Generate" Width="75" Height="25" HorizontalAlignment="Right" Name="generateButton" Click="generateButton_Click" Margin="0,5,87,5" />
        </Grid>
    </Grid>
</Window>
